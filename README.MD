# SAP

SAP (Scene Access Provider) is an extremely lightweight dependency injection plugin that implements the Provider pattern.

For C# users, consider Chickensoft's excellent [AutoInject](https://github.com/chickensoft-games/AutoInject) package.

